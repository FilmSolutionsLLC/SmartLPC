package com.fps.repository;

import com.fps.domain.AlbumPermissions;
import org.springframework.data.jpa.repository.JpaRepository;

public interface AlbumPermissionsRepository extends JpaRepository<AlbumPermissions,Long>{
}
